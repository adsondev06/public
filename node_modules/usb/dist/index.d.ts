import { WebUSB } from 'usb/dist/webusb';
import * as usb from 'usb/dist/usb';
/**
 * Convenience method to get the first device with the specified VID and PID, or `undefined` if no such device is present.
 * @param vid
 * @param pid
 */
declare const findByIds: (vid: number, pid: number) => usb.Device | undefined;
/**
 * Convenience method to get the device with the specified serial number, or `undefined` if no such device is present.
 * @param serialNumber
 */
declare const findBySerialNumber: (serialNumber: string) => Promise<usb.Device | undefined>;
declare const webusb: WebUSB;
export { usb, findByIds, findBySerialNumber, webusb };
export { Device, Transfer, DeviceEvents, getDeviceList, useUsbDkBackend, LibUSBException } from 'usb/dist/usb';
export * from 'usb/dist/usb/capability';
export * from 'usb/dist/usb/descriptors';
export * from 'usb/dist/usb/endpoint';
export * from 'usb/dist/usb/interface';
export * from 'usb/dist/webusb';
export * from 'usb/dist/webusb/webusb-device';
